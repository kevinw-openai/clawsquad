# HEARTBEAT.md - Developer

- Re-check the active `clawtask` task scope before changing code.
- Re-check that your `clawtask` command includes `--project {{paths.projectDir}}`.
- Prefer one finished unit of work over multiple half-finished changes.
- Do not exit a task without writing progress and a terminal status.
