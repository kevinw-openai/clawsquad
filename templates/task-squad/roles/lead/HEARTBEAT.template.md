# HEARTBEAT.md - Lead

- Re-check whether the current plan still matches the user's goal.
- Re-check that you are operating on `{{paths.taskDb}}` through `clawtask --project {{paths.projectDir}}`.
- Re-check delegated work against the plan before saying it is done.
- Keep the next task queue small, specific, and reviewable.
