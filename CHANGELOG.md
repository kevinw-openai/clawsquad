# Changelog

## 0.1.0

- Initial TypeScript CLI with `init`, `render`, `doctor`, and `apply`
- Scaffolded `main`, `manager`, `developer`, and `qa` squad roles
- Added strict template rendering with missing-token failures
- Added managed workspace file cleanup and backup-aware apply flow
- Added bindings support in the squad manifest
- Added richer workspace templates including `IDENTITY.md`, `TOOLS.md`, and `HEARTBEAT.md`
